{
  "mcpServers": {
    "gti": {
      "type": "stdio",
      "command": "uvx",
      "args": ["--from", "gti-mcp==0.1.3", "gti_mcp"],
      "env": {
        "VT_APIKEY": "${user_config.vt_apikey}"
      }
    }
  }
}
